# Python dependencies for local development and testing
# Note: AWS Glue uses its own PySpark environment; these are for local development only

# Core AWS libraries
boto3>=1.34.0
botocore>=1.34.0

# PySpark (updated to patched version for security)
# Note: AWS Glue 4.0 uses PySpark 3.3.0, but for local dev we use 3.3.3+ for security
pyspark>=3.3.3

# AWS Glue libraries (for local testing)
# Note: Some Glue libraries are not available via pip
# Use AWS Glue Docker containers for full local testing

# Data processing
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=12.0.0

# Database connectors (for local testing)
psycopg2-binary>=2.9.0  # PostgreSQL/Redshift
teradatasql>=17.20.0    # Teradata
google-cloud-bigquery>=3.11.0  # BigQuery

# Development and testing
pytest>=7.4.0
pytest-cov>=4.1.0
moto>=4.2.0  # Mock AWS services
black>=23.7.0  # Code formatting
pylint>=2.17.0  # Linting
mypy>=1.5.0  # Type checking

# Infrastructure
python-terraform>=0.10.1

# Utilities
python-dotenv>=1.0.0
pyyaml>=6.0
